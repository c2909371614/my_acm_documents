#include<cstdio>
#include<stream>
#include<string>
#include<cstring>
#define ll long long
#define FAST_IO std::ios::sync_with_stdio(false);
using namespace std;
const int N = 1e5 + 5;
bool my_check(char s1[], char s2[]){
	for(int i = 0; i < strlen(s1))
}
int mian(){
	char s[N], s1[N];
	int n;
	scanf("%d", &n);
	for(int i = 0; i < n; i++){
		cin >> s >> s1[N];
		int len = strlen(s), len1 = strlen[s1];
		if(!strcmp(s, s1)) printf("jntm!\n");
		else if(len == len1 ) printf("friend!\n");
		else if()
	} 
}
