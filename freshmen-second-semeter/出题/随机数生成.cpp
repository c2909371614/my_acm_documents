#include <bits/stdc++.h>
using namespace std;
const ll N = 1000000000;
const ll M = 100000;
long long n=0x3f3f3f3f3f,m=1;   //数据范围
double random()  //生成介于0-1之间的随机数
{
    return (double)rand() / RAND_MAX;
}
 
ll random(ll m) //生成就与0-m-1之间的随机数
{
    return (ll)(random()*(m-1)+0.5);
}
 
int main()
{
	fstream out("gohome.in",ios::out);
	
}

